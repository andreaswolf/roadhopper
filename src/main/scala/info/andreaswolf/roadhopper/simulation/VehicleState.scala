package info.andreaswolf.roadhopper.simulation

/**
 *
 * @param currentAcceleration The acceleration in meters per square second (m/(s^2^))
 * @param currentSpeed The speed in meters per second
 */
class VehicleState(val currentAcceleration: Double, val currentSpeed: Double) {

}
